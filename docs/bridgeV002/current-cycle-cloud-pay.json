{
  "last_handoff": 70,
  "title": "V2.3 — Direct/command_builder path model fix (CLOSED: verdict APPROVED, committed Father 74a1179, pushed; review01/review02 now use declared models)",
  "flow": "cloud_pay",
  "active_role": "archi01pay",
  "design_notes": "Handoff 70 is COMPLETE. V2.3 — Direct/command_builder path fix was implemented by imple01pay (Father-only), passed review01pay (PASS, no findings) + review02pay verdict (APPROVED, clean), committed + pushed by the Human on 2026-07-09.\n\nCommit: Father 74a1179 (2 files, +72/-1) pushed (0b687a6..74a1179). Clean staging — bridge_lib.py + start_coding.py only; command_builder.py UNCHANGED (verified empty diff); pre-existing files correctly excluded.\n\nV2.3 delivered: fixes the SAME `opencode --model` TUI-ignored bug (OpenCode 1.17.16) for DIRECT opencode roles — the bug V2.2 fixed for allocator-managed roles (imple01). Direct roles (review01, review02) used to fall back to global config-default (qwen3.6:27b) instead of their declared default_model; now they use their declared models.\n\n- bridge_lib.py: new `ensure_opencode_model_field(path, model_field)` helper. Atomic temp+rename write, preserves all existing top-level keys ($schema/permission/mcp/other providers), adds $schema only when missing, early return when model already matches.\n- start_coding.py direct-branch: after command_builder.build_start_command returns cmd_obj, before run_cmd_in_session, when runtime==opencode, extracts argv element after '--model' (reuses command_builder prefix logic — NO duplication) and calls ensure_opencode_model_field. try/except (OSError/JSONDecodeError/IndexError) + warning+continue.\n- command_builder.py UNCHANGED (read cmd_obj only). Allocator-branch (V2.2) UNCHANGED. No model-allocator changes (direct roles are Father-only).\n\n6 Architect decisions baked into handoff 70: (1) fix in start_coding direct-branch for runtime==opencode; (2) model-field from cmd_obj argv (no duplication); (3) atomic merge write preserving existing keys; (4) try/except + continue; (5) scope: direct-branch + opencode only, command_builder + allocator-branch unchanged, no model-allocator; (6) live validation.\n\nIMPORTANT correction (implementer found): archi01 has default_runtime=claude (Claude Code, not OpenCode) — so V2.3's opencode.json write correctly does NOT apply to archi01. The direct opencode roles are review01 + review02. (Handoff text assumed archi01 was opencode; implementer handled the actual runtime config correctly.)\n\nLIVE-VALIDATED: review01 opencode.json has model: ollama/ornith35b-q5-64k, review02 has model: ollama/qwen3.6:27b-q4_K_M, existing keys preserved. Test prompt to review01 → OpenCode showed `Build · ornith35b-q5-64k Ollama (local)`, /api/ps confirmed ornith35b-q5-64k:latest loaded. Direct roles now use declared models.\n\nbridgeV002 invariant preserved: ZERO Father files beyond bridge_lib.py + start_coding.py touched by V2.3 (verified by review01pay + review02pay + result). command_builder.py/dispatch.py/init_db.py/seed_bridge.py/app.py/config.py UNCHANGED.\n\nFull model-selection bug now CLOSED: V2.2 (allocator roles) + V2.3 (direct opencode roles). All OpenCode roles (imple01 allocator; review01/review02 direct) now use their declared models via the opencode.json model field, not the ignored --model flag.\n\nKNOWN GAPS still open (non-blocking): (1) EQUIVALENCE GAP — allocator run omits CLAUDE_CODE_MAX_OUTPUT_TOKENS + absolute binary path (Machine Profile not read); now minor since model-selection works via opencode.json. (2) Design limitation: llama.cpp adapter cannot adopt already-running external server. (3) archi01 is Claude Code — the --model flag for Claude Code TUI may or may not work (separate question, not addressed by V2.2/V2.3 which are opencode-specific).\n\nNext (awaiting Human): (a) V3 — bridgeV002 UI (model_source dropdown + alias picker + validation/runtime cards) + optional FastAPI; (b) Father hygiene — commit pre-existing uncommitted files (431, seed_bridge, dpmtf-app.js) separately; (c) verify archi01 (Claude Code) model-selection works (the --model flag for claude TUI is a separate question); (d) pause — model-allocator + model-selection bug are in a strong, fully live-validated state.",
  "verification_checklist": [],
  "open_gaps": [
    "V2.3 COMPLETE (handoff 70): Father 74a1179 committed + pushed. active_role back to archi01pay. Direct opencode roles (review01, review02) now use declared models. Model-selection bug FULLY CLOSED (V2.2 allocator + V2.3 direct).",
    "EQUIVALENCE GAP (verdict 66 note 2): allocator run omits CLAUDE_CODE_MAX_OUTPUT_TOKENS + absolute binary path (Father Machine Profile not read). Now minor since model-selection works via opencode.json model field (V2.2+V2.3). Still a minor env difference for allocator roles.",
    "Design limitation (NOT fixed): V2's llama.cpp adapter cannot ADOPT already-running external llama-server (status/stop read only adapter's own PID file).",
    "archi01 (Claude Code) VERIFIED 2026-07-09: `claude --model <model>` IS a valid Claude Code TUI flag (verified via `claude --help`) — UNLIKE OpenCode, it is NOT ignored. archi01's declared model qwen3.6:35b-a3b-64k confirmed loaded in Ollama via /api/ps. No model-selection bug for Claude Code roles — no fix needed. The OpenCode --model bug (V2.2+V2.3) was OpenCode-specific.",
    "Father repo still has uncommitted pre-existing files (not V2.3): 431_TRADE_TREND01.md, seed_bridge.py, static/js/dpmtf-app.js, databases/dpmtf.db (runtime), docs/bridgeV002/current-cycle-cloud-pay.json (cycle-state). Human decides when to commit Father hygiene separately. (opencode-roles/*.json are in ~/.config/opencode-roles/ — separate git repo.)",
    "V3 not started: bridgeV002 UI integration (model_source dropdown + alias picker + validation/runtime cards + CUDA0/1 display + persistent last validation result), optional FastAPI service, AI PC Resource WebUI integration. Backend API auto-surfaces new columns via SELECT *.",
    "cloud_pay bridge_id_counters.next_id still 49 (bookkeeping bug) — handoff 70 used --id 70 explicitly. Next handoff should use --id 71 explicitly.",
    "dispatch.py post-dispatch hang (separate known bug): signal-send dispatch may hang; use timeout + verify injection via tmux capture-pane.",
    "UI/Frontend/i18n never-drop rule (Father): permanent exclusions. V2.3 touched no frontend."
  ],
  "branch": "master",
  "commit": "Father 74a1179 + model-allocator 8b0451e (both pushed)",
  "updated": "2026-07-09T17:00:00Z"
}
